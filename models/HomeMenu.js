class HomeMenu {
    constructor(id, title, color, routerPath){
        this.id = id;
        this.title = title;
        this.color = color;
        this.routerPath = routerPath;
    }
}

export default HomeMenu;